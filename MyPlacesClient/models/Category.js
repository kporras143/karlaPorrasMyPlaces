class Category {

    constructor(prefix, suffix, id, name, primary, pluralName, shortName) {
        this.prefix = prefix;
        this.suffix = suffix;
        this.id = id;
        this.name = name;
        this.primary = primary;
        this.pluralName = pluralName;
        this.shortName = shortName;
    }
}